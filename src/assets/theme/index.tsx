import { createTheme } from '@mui/material'

const theme = createTheme({
  colors: {
    darkBlue: '#1B1F38',
    ligthRed: '#E44C4E',
    white: '#fff',
    link: '#F7931B',
  },
})

export default theme
